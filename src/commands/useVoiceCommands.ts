import Discord, { Message } from 'discord.js'

export const useVoiceCommands = (message: Message, command:Icommand) => {
    switch (command.commands[0]){
        case 'play':
            
        break
    } 
}